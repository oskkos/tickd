# Cloudflare Pages. Rules apply only to requests that miss a static asset.
#
# Client-side routes must resolve to the shell so deep links work.
#
# Do NOT add a top-level 404.html alongside this: Pages falls back to index.html only when the
# project has no 404.html, and adding one turns every unmatched route — including valid client-side
# routes — into a 404. Verified empirically on a preview deployment.
/*  /index.html  200
